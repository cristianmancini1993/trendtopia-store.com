# Include inside server { } for trendtopia-store.com (after root is set).
# Exact match takes precedence over location / { try_files ... }.

location = / {
    try_files /home/index.$home_locale.html /home/index.en.html =404;
}
